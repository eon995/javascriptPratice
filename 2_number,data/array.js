const a1 = [1, 2, 3, 4]; // 숫자로 구성된 배열

const a2 = [1, 'tow', 3, null]; // 여러가지로 구성돈 배열

const a3 = [
    { naem: 'eon', pas: 123 },
    { name: 'eon1', pas: 1234 }
];

const a4 = [[1, 2, 3], [4, 5, 6]];// 배열안의 배열

console.log("array -------------------------");

const arr = ['a', 'b', 'c'];

console.log(arr.length); //배열의 길이

const arr0 = arr[0];
const arrLast = arr.length - 1;

console.log(arr0);

console.log(arr.length - 1);

console.log(arr[arr.length - 1]); // 배열의 마지막 값


